name: Daily Job

on:
  schedule:
    - cron: '* * * * *'

jobs:
  daily-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run script
        run: node leo.js
